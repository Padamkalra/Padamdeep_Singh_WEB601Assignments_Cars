import { HoverAffectDirectiveSpecTsDirective } from './hover-affect.directive.spec.ts.directive';

describe('HoverAffectDirectiveSpecTsDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverAffectDirectiveSpecTsDirective();
    expect(directive).toBeTruthy();
  });
});
